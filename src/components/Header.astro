---

---

<header class="relative w-full h-screen overflow-hidden">
  <nav class="z-50 w-full" id="navbar">
    <!-- contenedor -->
    <div
      class="container mx-auto flex justify-between items-center px-10 py-14 h-20">
      <!--  logo -->
      <div class="logo">
        <a href="/"><img src="./img/logo.png" alt="" /></a>
      </div>
      <!-- menu -->
      <ul class="flex space-x-10 font-semibold text-white items-center">
        <li>
          <a href="#">Servicios</a>
        </li>
        <li>
          <a href="#">Contáctenos</a>
        </li>
        <li>
          <a href="#"><img src="./img/call.png" alt="" /></a>
        </li>
      </ul>
    </div>
  </nav>
  <!-- contenido -->
  <div class="flex flex-col justify-center h-full">
    <h1 class="text-white text-5xl font-bold text-center mt-20">
      <span class="block">BARRIDO INDUSTRIAL EN PERÚ</span>
      <span class="block">BARREDORA PERU</span>
    </h1>
    <p class="text-white text-center mt-5">
      La solución eficiente y profesional para limpiar grandes áreas
      industriales y comerciales.
    </p>
    <video autoplay muted id="background-video" loop
      ><!--  -->
      <source src="/img/background-header.mp4" type="video/mp4" />
    </video>
  </div>

  <style>

    ul li {
      position: relative;
      padding-left: 10px; /* Ajusta el espacio a la izquierda del texto */
    }
    ul li:not(:first-child)::before {
      content: "";
      position: absolute;
      top: 50%;
      left: 0;
      transform: translateY(-50%);
      width: 2px; /* Ancho de la línea vertical */
      height: 50%; /* Altura de la línea vertical */
      background-color: white; /* Color de la línea vertical */
    }
    #navbar {
      z-index: 1;
      background: linear-gradient(to bottom, rgb(0, 0, 0), transparent);
    }
    #background-video {
      position: absolute;
      width: 100%;
      height: auto;
      overflow: hidden;
      object-fit: cover;
      top: 0px;
      z-index: -1;
    }
  </style>
</header>
